PORT=3010
# Optional: share one state directory across multiple local checkouts.
# AGENT_SWARM_DATA_DIR=/absolute/path/to/shared-agent-swarm-data
AGENTMAIL_API_KEY=
# Optional Codex swarm defaults:
# SWARM_CODEX_MODEL_LEAD=gpt-5.4
# SWARM_CODEX_MODEL_WORKER=gpt-5.4
# SWARM_CODEX_REASONING_EFFORT=xhigh
# Optional: override where per-agent memory checkpoints are stored.
# AGENT_SWARM_MEMORY_DIR=/absolute/path/to/.agent-swarm/memory
